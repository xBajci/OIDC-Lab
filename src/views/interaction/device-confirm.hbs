<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{#if title}}{{title}} - {{/if}}OIDC Lab</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-indigo-100 to-purple-100 min-h-screen flex items-center justify-center p-4">
  <div class="w-full max-w-md">
    <div class="bg-white rounded-lg shadow-xl p-8">
      <div class="text-center mb-6">
        <h1 class="text-2xl font-bold text-gray-900">Authorize Device</h1>
        <p class="text-gray-600 mt-2">
          <strong>{{clientName}}</strong> is requesting access
        </p>
      </div>

      <div class="bg-gray-50 border border-gray-200 rounded-md p-3 mb-4 text-center">
        <span class="text-sm text-gray-600">Device Code:</span>
        <span class="font-mono font-bold text-lg text-gray-900 ml-2">{{userCode}}</span>
      </div>

      {{#if scope.length}}
      <div class="mb-6">
        <h2 class="text-sm font-medium text-gray-700 mb-3">This device will be able to:</h2>
        <ul class="space-y-2">
          {{#each scope}}
          <li class="flex items-center">
            <svg class="h-5 w-5 text-indigo-500 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <span class="text-gray-700">{{this}}</span>
          </li>
          {{/each}}
        </ul>
      </div>
      {{/if}}

      {{!-- oidc-provider's confirm form (hidden) — contains action, xsrf, user_code, confirm=yes --}}
      <div style="display:none">{{{formHtml}}}</div>

      <div class="flex space-x-3">
        <button
          type="button"
          id="authorizeBtn"
          class="flex-1 py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
        >
          Authorize
        </button>

        <button
          type="button"
          id="denyBtn"
          class="flex-1 py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
        >
          Deny
        </button>
      </div>

      <div class="mt-6 text-center text-sm text-gray-500">
        <p>Client ID: <code class="bg-gray-100 px-1 rounded">{{clientId}}</code></p>
      </div>
    </div>
  </div>

  <script>
    (function () {
      var providerForm = document.getElementById('op.deviceConfirmForm');

      // Authorize — submit the provider's form as-is (has confirm=yes)
      document.getElementById('authorizeBtn').addEventListener('click', function () {
        if (providerForm) providerForm.submit();
      });

      // Deny — remove 'confirm' hidden input and add 'abort' before submitting
      document.getElementById('denyBtn').addEventListener('click', function () {
        if (!providerForm) return;
        var confirmInput = providerForm.querySelector('input[name="confirm"]');
        if (confirmInput) confirmInput.remove();
        var abortInput = document.createElement('input');
        abortInput.type = 'hidden';
        abortInput.name = 'abort';
        abortInput.value = 'yes';
        providerForm.appendChild(abortInput);
        providerForm.submit();
      });
    })();
  </script>
</body>
</html>
